package INHERITENCE;

public class IN {
    public static void main(String[] args) {

        measurements measureRef = new box();
        box actualBox = new box();

        // measureRef.dis(); // Not allowed because through measureRef
        // we can only access the members of measurements.

    }
}